{
  "name": "sanchoy-app-monorepo",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd server && npm install && npx prisma generate"
  }
}
